//Finding odd numbers from m to n 

function findOddNumber(m, n) {
    for (let i = m; i <= n; i += 2) {
        console.log(i);
    }
}

const firstNumber = 50;
const lastNumber = 80;
findOddNumber(firstNumber, lastNumber);