// Find the sum of random three mumbers

function findSumOfThreeNumbers(x, y, z) {
    const total = x + y + z;
    return total;
}

const firstNumber = 10;
const secondNumber = 20;
const thirdNumber = 30;
const output = findSumOfThreeNumbers(firstNumber, secondNumber, thirdNumber);
console.log(output);